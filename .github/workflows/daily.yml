name: 每日股东大会数据更新

on:
  # 每天 UTC 15:00 = 北京时间 23:00
  schedule:
    - cron: "0 15 * * *"
  # 允许手动触发(测试用)
  workflow_dispatch:
    inputs:
      date:
        description: '抓取日期(YYYY-MM-DD,留空=昨天)'
        required: false
        default: ''

permissions:
  contents: write   # 需要写权限以 push 数据回仓库

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install pypdf openpyxl

      - name: 下载最新数据库(从上次成功运行的 artifact)
        uses: actions/download-artifact@v4
        with:
          name: cninfo-db
          path: data/
        continue-on-error: true   # 首次运行没 artifact,允许失败(用 git 里的 db)

      - name: 确保 db 存在
        run: |
          mkdir -p data
          if [ ! -f data/cninfo.db ]; then
            echo "::warning::没有 cninfo.db,从 git checkout 内容建一个空 db"
            # 触发 daily_update.py 里的 CREATE TABLE IF NOT EXISTS 逻辑
            touch data/cninfo.db
          fi
          ls -la data/

      - name: 运行每日更新
        env:
          DB_PATH: ./data/cninfo.db
          HTML_OUT: ./dist/index.html
          WORK_DIR: ./work
          EXPIRE_DAYS: "0"
        run: |
          mkdir -p data dist work
          # 支持手动指定日期
          if [ -n "${{ inputs.date }}" ]; then
            python daily_update.py "${{ inputs.date }}" || true
          else
            python daily_update.py || true
          fi
          # 即使 update 失败,也要保证 dist/index.html 存在(供 Pages 部署)
          if [ ! -f dist/index.html ] && [ -f data/cninfo.db ]; then
            echo "::warning::update 失败,尝试直接 build_calendar"
            DB_PATH=./data/cninfo.db HTML_OUT=./dist/index.html python build_calendar.py || true
          fi

      - name: 显示摘要
        if: always()
        run: |
          echo "## 📊 更新摘要" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          ls -la data/ dist/ 2>&1 | head -10 >> $GITHUB_STEP_SUMMARY || true
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f data/cninfo.db ]; then
            python -c "
          import sqlite3
          con = sqlite3.connect('data/cninfo.db')
          try:
              n = con.execute('SELECT COUNT(*) FROM meeting').fetchone()[0]
              print(f'数据库总会议数: {n}')
              for r in con.execute('SELECT source_run, COUNT(*) FROM meeting GROUP BY source_run ORDER BY source_run'):
                  print(f'  {r[0]}: {r[1]} 场')
          except Exception as e:
              print(f'查询失败: {e}')
          " >> $GITHUB_STEP_SUMMARY || true
          fi

      - name: 上传数据库(供下次下载)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cninfo-db
          path: data/cninfo.db
          retention-days: 90
          if-no-files-found: warn

      - name: 上传 HTML(供 Pages 部署)
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: update
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
